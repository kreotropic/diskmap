# SPDX-FileCopyrightText: 2026 Ricardo Ferreira <rsfneg@gmail.com>
# SPDX-License-Identifier: AGPL-3.0-or-later

# Everything listed here stays out of the App Store tarball built by
# `krankerl package`.

# Dependencies / build inputs (js/ ships pre-built)
node_modules
vendor
src
build
webpack.config.js
babel.config.js
package.json
package-lock.json
composer.json
composer.lock
js/*.map

# Tests & CI
tests
phpunit.xml
.phpunit.result.cache
phpstan.neon
.github

# Dev tooling & repo metadata
.git
.gitignore
.nextcloudignore
.vscode
krankerl.toml
l10n/*.bak

# .reuse/ and REUSE.toml deliberately stay IN the tarball: the licensing of
# js/*.js, l10n/*.js and img/*.svg is declared there rather than in-file, so
# dropping them would make the shipped artifact non-REUSE-compliant on its own.

# Docs assets only needed on GitHub / the App Store crawler
screenshots
blueprint.json
